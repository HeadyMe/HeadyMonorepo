services:
  - type: web
    name: heady-systems
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node heady-manager.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"